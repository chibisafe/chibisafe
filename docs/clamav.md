### ClamAV binary/daemon installation instructions

Fedora based distributions:

```
sudo yum install clamav
```

Debian based distributions:

```
sudo apt-get install clamav clamav-daemon
```

For OS X:

```
sudo brew install clamav
```
